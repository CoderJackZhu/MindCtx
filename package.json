{
  "name": "mindctx",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @mindctx/core test",
    "test:watch": "pnpm --filter @mindctx/core test:watch",
    "typecheck": "pnpm -r typecheck",
    "release:obsidian": "node scripts/bump-version.mjs obsidian",
    "release:vscode": "node scripts/bump-version.mjs vscode"
  },
  "devDependencies": {
    "typescript": "^5.4",
    "@types/node": "^20"
  }
}
