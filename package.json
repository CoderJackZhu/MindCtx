{
  "name": "minddoc",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @minddoc/core test",
    "test:watch": "pnpm --filter @minddoc/core test:watch",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4",
    "@types/node": "^20"
  }
}
